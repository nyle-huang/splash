# Azure Functions deployment dependencies for the public broker.
# The package source is deployed with the function app; the broker only needs
# these HTTP/runtime dependencies, not the full generation stack.
azure-functions>=1.21,<2.0
fastapi>=0.115,<1.0
httpx>=0.27,<1.0
pydantic>=2.8,<3.0
